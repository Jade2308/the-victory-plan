VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
# Optional: the exact URL users should return to after Google or email sign-in.
# If omitted, the current site's URL (including Vite's base path) is used.
VITE_AUTH_REDIRECT_URL=https://your-app-domain.example/
